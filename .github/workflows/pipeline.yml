name: Continous Integrations

on:
push:
  branches: ["Desafio03", "main"]
pull_request:
  branches:
    - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Instalar dependências
        run: npm install
      - name: Rodar os testes
        run: npm test
